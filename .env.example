# Symfony Application Environment Configuration
# Copy this file to .env and adjust the values for your environment

###> symfony/framework-bundle ###
# Environment: dev, prod, test
APP_ENV=prod
# Generate a new secret for production: php bin/console secrets:generate-keys
APP_SECRET=CHANGE_ME_TO_RANDOM_STRING
###< symfony/framework-bundle ###

###> symfony/routing ###
# Configure how to generate URLs in non-HTTP contexts, such as CLI commands.
# For production, set this to your actual domain
DEFAULT_URI=http://localhost
###< symfony/routing ###

###> doctrine/doctrine-bundle ###
# Database connection URL
# Format: mysql://username:password@host:port/database?serverVersion=8.0&charset=utf8mb4
# 
# For Docker:
DATABASE_URL="mysql://symfony_user:symfony_pass@mysql:3306/symfony_db?serverVersion=8.0&charset=utf8mb4"
#
# For local development:
# DATABASE_URL="mysql://root:password@127.0.0.1:3306/symfony_db?serverVersion=8.0&charset=utf8mb4"
#
# For production:
# DATABASE_URL="mysql://prod_user:prod_password@prod_host:3306/prod_db?serverVersion=8.0&charset=utf8mb4"
###< doctrine/doctrine-bundle ###
